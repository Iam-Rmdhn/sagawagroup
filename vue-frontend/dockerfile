# Stage 1: Build aplikasi menggunakan Node.js
FROM node:22 AS build

# Tentukan direktori kerja di dalam container
WORKDIR /app

# Salin file package.json dan package-lock.json
COPY package*.json ./

# Install dependensi menggunakan npm
RUN npm install

# Salin seluruh source code frontend
COPY . .

# Build aplikasi Astro (output di folder dist)
RUN npm run build

# Stage 2: Serve hasil build dengan Nginx
FROM nginx:alpine

# Salin hasil build dari stage pertama ke dalam direktori Nginx
COPY --from=build /app/dist /usr/share/nginx/html

# Expose port 80 untuk website
EXPOSE 80

# Nginx akan otomatis serve website pada port 80